name: VPS Free (Windows Ubuntu Latest)

on:
  schedule:
    - cron: '0 */5 * * *'  # Executar a cada 5 horas 
  workflow_dispatch:

jobs:
  criar-maquina:
    runs-on: windows-latest

    steps:
    - name: Criar Nova Máquina
      run: |
        choco install ngrok
        ngrok config add-authtoken 2bls1erTfKNc7xMti7oZ9RknU3l_XjnGJpxaZGnMGiPcMwhG
        net user golden P@ssw0rd /add
        net localgroup Administrators golden /add
        ngrok tcp 3389
